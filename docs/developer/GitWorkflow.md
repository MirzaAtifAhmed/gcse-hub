# Git Workflow

Use feature branches:

```bash
git checkout -b feature/sprint-01-foundation
```

Commit style:

```text
feat: add authentication foundation
fix: correct dashboard route
docs: add sprint documentation
```
