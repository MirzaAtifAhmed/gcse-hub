# Seed Demo Users

Adds:

```text
parent@gcsehub.local / Password123!
student@gcsehub.local / Password123!
```

Manual action needed: import and call `seedDemoUsers()` from `apps/api/src/seed/seed.ts`.
